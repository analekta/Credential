# Admin Password - CHANGE THIS TO A STRONG PASSWORD
ADMIN_PASSWORD=your-strong-password-here

# Session Secret - Generate a random value for production
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-this-to-a-random-value-in-production

# Environment Type - uncomment for production deployment
# NODE_ENV=production
